{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "//note": "preDeployCommand stays on db:push until the operator runs `bash scripts/prisma-deploy.sh` once against Neon (or sets MIGRATIONS_BASELINED=1). See scripts/prisma-deploy.sh header for the baselining steps. After that, switch this to `bash scripts/prisma-deploy.sh`.",
    "preDeployCommand": "npm run db:push",
    "startCommand": "npm run start:api",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
